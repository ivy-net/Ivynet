---
name: build dev backend

on:
  pull_request:
    branches:
      - dev
    paths:
      - ivynet-client

jobs:
  build:
    name: build client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt install -y rustc cargo protobuf-compiler pkg-config libssl-dev
      - run: cd ivynet/ivynet-client
      - run: cargo build --release
