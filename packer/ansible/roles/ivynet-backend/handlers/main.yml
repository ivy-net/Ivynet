---
- name: Reload daemon
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart service
  ansible.builtin.systemd_service:
    name: memcached
    state: restarted
    enabled: true
